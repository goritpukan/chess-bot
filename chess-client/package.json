{
  "name": "chess-client",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx esbuild index.js --bundle --outfile=dist/bundle.js --minify",
    "start": "npx serve ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@lichess-org/chessground": "^9.6.0",
    "chess.js": "^1.4.0"
  }
}
